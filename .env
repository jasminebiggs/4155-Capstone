# .env (at project root)
DB_USER=mydbuser
DB_PASSWORD=mysecretpassword
DB_HOST=localhost
DB_PORT=5432
DB_NAME=smart_buddy

# For cloud deployments like Render, use their provided DATABASE_URL instead
# DATABASE_URL=postgresql://username:password@host:port/database
